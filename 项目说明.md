# 课表项目 · 交接说明（V12 当前状态）

> 用途：以后新开 AI 对话时，把【给 AI 的话】整段复制粘贴给助手即可，不用再从头讲解。
> 本文件是给"人"看的完整背景；【给 AI 的话】是精简版指令。

---

## 一、给 AI 的话（复制这段发给助手）

```
下面是一个已开发完成的前端静态网站项目，请基于这份说明协助我后续的修改。

【项目概况】
- 纯前端静态站（HTML+CSS+JS），无后端。数据分两个文件并行 fetch：js/data.json（只课表）与 js/contacts.json（通讯录）。
- 部署在 GitHub Pages，账号 DDLZ43，仓库 -V12，分支 main，目录根即网站根。
- 本地路径 E:\桌面\课表项目V12。项目里"上传.bat"一键 add+commit+push 部署。

【页面结构】共 2 个页面：
1. index.html：课表速查助手。底部导航四个 tab（个人课表 / 班级课表 / 通讯录 / 统计），默认先显示「个人课表」。
   - 顶部居中有大标题"课表速查助手"；下方原"日期小标"行已清空仅作占位（两页抬头上方保留空副标）。
   - 逻辑在 js/app.js，数据通过 js/data-loader.js 注入全局 window.scheduleData / contactsData。
2. daike.html：代课管理助手。底部导航（代课 / 通讯录 / 导入）三个面板。
   - 代课：教师请假登记 + 代课通知生成。登记顺序：先选「请假事由」与「请假日期」→ 输教师姓名（联想选中即自动查课表）
     → 勾选需代课节次 → 点「生成记录(加入累积)」。累积池支持连续登记多位老师。
     （已去掉原独立「查询课程」按钮：输入教师名联想里点选即查询；若失焦时联想只剩唯一匹配则自动补查。）
   - 通讯录：只读教师通讯录（仅查看+可直接拨号），默认显示前 5 条、点「查看全部」展开；支持中文/拼音首字母搜索。无任何编辑能力。
      - 导入：批量导入 / Excel 粘贴，生成「纯课表」data.json（不含通讯录；名单独立在 js/contacts.json）
   - （已移除原「修改」面板：老师已不能通过 UI 改课表或改号码，需改数据请直接改 js/data.json（课表）/ js/contacts.json（名单），或先维护 tmp 名单后重新生成。）
   - 代课教师输入联想 initTeacherSuggest 在 js/daike.js，支持中文 + 拼音首字母（matchName）。

【公共资源】
- css/style.css：两页共用样式（含夏月清新色调优化、字体美化、数字等宽字体）。
- js/data-loader.js：并行 fetch js/data.json + js/contacts.json，统一注入 scheduleData/contactsData/SCHEDULE_DATA/CONTACTS_DATA，
  并提供 getAllClasses()/getAllTeachers()/getPinyinInitials()/matchName()/searchContacts() 等全局函数。
- js/data.json ：课表（scheduleData + periods + days），**不再含通讯录**。
- js/contacts.json ：通讯录 { contactsData:[{name,phone,note}] }，名单独立维护。
- js/app.js：index.html 逻辑。 js/daike.js：daike.html 代课工具逻辑。
- lib/xlsx.full.min.js：Excel 解析库（导出用）。

【设计风格约定（保持统一）】
- 主题色：夏月晴蓝系，主色 #3FA7D4，端点 #35A0CF/#2F96C6/#2B8BBB，浅端 #6BC6EA/#5DBBE0。
- 背景：淡蓝渐变（#F2F8FC → #E9F3F8）。
- 底部导航：胶囊浮起风格——激活项用蓝渐变 + 白字 + 圆角 + 软投影；容器毛玻璃半透明。
- 字体：中文用 PingFang SC / Microsoft YaHei；数字用等宽字体（SF Pro Display / Tahoma）。
- 课表表格：桌面居中、左右留白均匀；手机端横向滚动、节次列 position:sticky 固定。
- 下拉框匹配高亮文字用 #35A0CF（蓝色），不要用红色。

【提醒】
- 我已删除了 import.html 和 admin.html 两个旧页面，目前项目只有 index.html + daike.html 两个页面，
  不要再提或新建这两个页面。
- 底部导航的大标题不要放图形/emoji 图标，保持简洁文字。
```

---

## 二、项目详情

### 这是什么
部署在 GitHub Pages 的静态站，共 2 个页面，统一由 `js/data.json`（课表）+ `js/contacts.json`（名单）供数：

| 页面 | 文件名 | 名称 | 功能 |
|------|--------|------|------|
| 首页 | index.html | 课表速查助手 | 班级课表 / 个人课表 / 教师通讯录 / 统计数据 |
| 代课 | daike.html | 代课管理助手 | 代课通知工具 / 只读教师通讯录 / 批量导入导出 |

### 网址（老师手机/电脑直接打开）
```
课表速查助手   https://ddlz43.github.io/-V12/
代课管理助手   https://ddlz43.github.io/-V12/daike.html
```

### 数据文件（名单独立，data.json 只留课表）
- `js/data.json`（课表）：`scheduleData`（70 班）+ `periods` + `days`。改课表只碰这一文件。
- `js/contacts.json`（名单）：`{ "contactsData": [ {name, phone, note} ] }`（现 233 位）。改号码/增减教师只碰这一文件。
- 原则：课表与通讯录解耦——新学期换课表不丢名单、改名单不影响课表。改完走"上传.bat"发布。

### 页面结构（底部导航）
- index.html：个人课表 | 班级课表 | 通讯录 | 统计　（底部导航「个人课表」在首位且默认先显示；班级/个人可通过点 tab 切换）
- daike.html：代课 | 通讯录 | 导入　（默认「代课」）

### 平时改课表 / 改数据怎么做（★自「修改」面板移除后，数据只能离线改）
1. **改课表**：直接编辑 `js/data.json` 里 `scheduleData` 对应班级/节的 `{course, teacher, room}`；亦可走 daike「导入」面板用 Excel 批量生成后替换。
2. **改教师号码 / 增减教师**（改权限 `js/contacts.json`）：
   - 直接编辑 `js/contacts.json` 里 `contactsData` 对应条目；或
   - 维护 `tmp_new_contacts.txt`（`姓名\t手机号`，一行一人）→ 交给 AI 重建 `js/contacts.json` 后替换。
   - 说明：`tmp_new_contacts.txt` 仅是维护清单，**不会自动生效**，须把重建结果写回 `js/contacts.json` 才上线。
3. **发布**：双击「上传.bat」一键上传 GitHub → 等 1~2 分钟 → 强刷（Ctrl+F5）。
   - 改课表后 index/daike 所有页自动用新数据；改通讯录后 index 通讯录与 daike 通讯录同步生效。

### 每学期换新课表
- 用 daike.html「导入」面板上传/粘贴 Excel → 生成的 `data.json` 是**纯课表**（scheduleData/periods/days），替换发布即可。
- 名单独立在 `js/contacts.json`，不受换课表影响、不会重算丢失；教师名与课表教师按 name 关联仅供展示，不合并进 data.json。

### 上传 / 发布
- 只有"改完数据要上线"才需要。双击「上传.bat」，自动 add+commit+push。
- 上传需能连 GitHub 的网络（国内不稳，可开代理或重试）。网站本身不需要代理。

### 常见问题
- 打开是旧数据/空白 → 浏览器缓存，强刷 Ctrl+F5 或加 ?v=数字 绕过缓存。
- 上传失败 → 网络问题，开代理或过会儿重试。
- 电脑上 .git 文件夹显示"只读" → 正常，Git 保护机制，别取消只读。

---

## 三、环境信息
```
GitHub 账号：DDLZ43
仓库：-V12（https://github.com/DDLZ43/-V12）
分支：main
远端：https://github.com/DDLZ43/-V12.git
本地路径：E:\桌面\课表项目V12
Git 凭据已配置（token 方式），日常双击「上传.bat」即可。
```

## 四、文件结构（项目根目录）
```
index.html          课表速查助手（首页）
daike.html         代课管理助手（代课工具 + 只读通讯录 + 导入）
css/style.css       样式（公共，夏月清新蓝 + 字体优化）
js/data.json        课表数据（scheduleData + periods + days），不含通讯录
js/contacts.json    通讯录（contactsData，现 233 位），名单独立维护
js/data-loader.js   并行加载 data.json + contacts.json，注入全局变量与工具函数
js/app.js           index.html 逻辑
js/daike.js         daike.html 代课工具逻辑
lib/xlsx.full.min.js Excel 解析库
上传.bat            一键上传脚本（本地用，不上 GitHub）
项目说明.md         本交接说明（即 PROJECT_BRIEF，唯一主说明；新开对话直接整段教给助手第一节）
```

## 五、已完成的近期优化清单
- 删除 import.html / admin.html 两个旧页面（避免再次提起或新建）。
- 标题改名：index=课表速查助手、editor=代课管理助手；标题居中、日期靠下。
- 整体夏月清新色调：背景淡蓝渐变、蓝绿系统一、中文字体 + 等宽数字字体。
- 底部导航统一为"胶囊浮起"风格，两页一致，显眼且不突兀。
- 课表表格对齐：桌面居中留白均匀、手机横向滚动、节次列 sticky、容器去大边框。
- 下拉框高亮文字由红色改为蓝色 #35A0CF。
- 代课/个人课表教师输入联想支持"拼音首字母"映射（用 window.matchName / getPinyinInitials）。

## 六、本项目最近一次重大升级（请基于此态继续开发）
> ⚠️ 本节与稍后「第八、待办」之外的最新代课逻辑，统一以文末「十、代课通知与累积结构（本地 js 现状）」为准。第六节里凡提到累积 key 为 6 段、标题带日期、请假带前缀/对齐、星期与班级同行的旧描述均已作废，勿再据此开发或答疑；最新请直接看第十节。

- 【代课登记】顶部改为两层：上层「请假事由」下拉 + 「请假日期」并排；下层「教师姓名」输入（保留联想）。
  手机端（≤768px）改上下竖排、三框满宽同高：请假日期框已做 `appearance:none` + 左对齐，
  确保与请假事由、教师姓名等宽且视觉上不像"短一截"（修复细节见第八节第10条）。
- 请假事由下拉选项（自上而下）：请选择请假事由(占位空默认) / 事假 / 病假 / 培训 / 婚假 / 产假 / 陪护假。
- 事由必填：默认占位空；未选时点「生成记录」拒绝并提示「请先选择请假事由」；生成成功后下拉复位回占位空。
- 累积记录 key 现为 6 段：`num|cls|teacher|bz|day|reason`（班次|班级|教师|是否班主任|星期|请假事由）。
- 已删除独立「查询课程」按钮：点联想选中或按回车即查询；输入失焦且联想只剩唯一匹配时自动查询兜底。
- 代课通知新排版（每年级一段）——
    例：
    【二年级  代课通知  2025/9/15   】
    请假教师：张一、李二、杨四 外出培训
              王三 病假
    
    星期一 二1班 第1、2节
          二3班 第4节
    星期二 二1班 第3、4节
          二2班 第2节
    辛苦转发通知至年级组内。
    规则：标题不再含请假教师；「请假区」按请假事由分组（同事由合并成行、姓名用顿号连接），
    首行带「请假教师：」前缀，后续不同事由行用 5 个全角空格占位使各姓名首字对齐，事由紧贴名后不另对齐；
    「请假区」与「节次区」之间空一行；「节次区」沿用原逻辑（同星期第一次写星期、后续班级星期用空格占位对齐班号、
    先按星期后按班号升序排列、连续节次合并如"第1、2节"，代班主任加" 代班主任"）。
- 累积池导出 Excel：D 列（每条记录第 4 列）现会填入对应请假事由，事由取自累积 key 第 6 段（见第八节第 11 条）。

---

## 七、重要约定与注意事项
- **data.json 编码**：是 UTF-8 中文，替换务必保持同编码，否则乱码。
- **班级命名固定**「汉字年级+数字」（一1、二10…），别改成阿拉伯年级（年级用汉字"一~六"，班号为数字）。
- **每天固定 6 节**（个别第 7 节需求需另做处理）。
- **通知复制已做手机端三级降级兼容**：Async Clipboard / execCommand / 手动复制兜底条（见 js/daike.js copyNotif → legacyCopy → showManualCopy）。
- **同步约定**：一切未来改动 / 每次新开对话的重要信息，务必同步更新到 PROJECT_BRIEF.md（本文件，唯一主说明）。

## 八、版本迭代记录（V12）
1. index 四 tab / editor 三面板（代课 + 修改 + 导入）。
2. 代课模块：请假登记＋勾选＋累积池＋通知一键复制＋Excel 导出。
3. 教师搜索中文/电话/拼音首字母联想（matchName / getPinyinInitials）。
4. editor 默认「代课」，支持 `#代课` / `#修改` 锚点直达。
5. 复制兼容多级降级修复手机端（Async Clipboard / execCommand / 手动复制）。
6. 通讯录手机端宽度与底部导航对齐、输入框可收缩防撑宽。
7. 课表可视化编辑一键导出 data.json。
8. 原始课表导入自动合并旧电话（data-loader 异步加载、一处改处处同步）。
9. 【本次】代课升级：
   - 新增「请假事由」必选下拉 + 与日期并排；
   - 移除原「查询课程」按钮、改失焦唯一匹配自动查；
   - 累积 key 追加 `reason`；通知按请假事由分组重排（见第六节）。
10. 【手机端】代课顶部「请假日期」框宽度与左对齐修复：
   - 根因：`input[type=date]` 在 iOS/安卓 WebKit 里是系统自带控件，自身有固定宽度，
     只设 `width:100%` 不足以铺满，看起来比上下两个自绘框（请假事由下拉 / 教师姓名）"短一截、值居中被挤偏"。
   - 修复（daike.html 移动端 @media 内，仅作用于本页 date，不影响其它输入）：
     - 「请假日期」加 `-webkit-appearance:none; appearance:none;` → 解除系统控件固有宽度，
       连同 `width:100% !important` + `box-sizing:border-box` 真正与上下框等宽；
     - 用 `::-webkit-date-and-time-value / ::-webkit-datetime-edit` 设 `text-align:left` 让日期字靠左，
       与下拉、教师姓名文字的左起点对齐；
     - 保留点击仍能正常弹出系统日期选择器（只是去掉了框内自带小日历图标）。
   - 通用经验：之后再遇到"手机端系统控件（date/time 等）宽度不对 / 不贴左"的问题，先加 `appearance:none` 再统一盒模型即可。
11. 【导出】累积池导出 Excel 的 D 列填入对应请假事由：
    - 背景：累积 key 现为 6 段（末段 `reason` 请假事由），此前 Excel 导出 D 列（每条记录第 4 列）为空 `''`。
    - 修复（js/daike.js `doExportExcel`）：从 key 第 6 段取 `p[5]` 作为 `reason`，填到该条 row 的 D 列
      （即 `[日期, 星期, 教师, <reason>, 班级, 节次, ...]` 中原来的空字符串位置）。
    - 省心前提（与用户确认为事实）：同一教师/同天/同班/同节不会用多种事由重复登记，故 D 列取值无歧义；
      每条记录填写其生成时所选的那一个请假事由（事假/病假/培训等）。
12. 【导出】A 列真实请假日期与星期一一对应（方案A）、H 列保留 today、日期改斜杠不带零：
    - 需求：导出 Excel 里 A 列日期要与 B 列星期对上；A 日期用 `YYYY/M/D` 不带零；H 列保留原 today。
    - 背景：旧逻辑 A 列直接复用请假日期框值（`YYYY-MM-DD`）；多选星期时 A 无法与星期对应。
    - 修复（js/daike.js `doExportExcel`）：
      - 以请假日期框为「基准日」，拆出 Y/M/D 用 `new Date(Y, M-1, D)` 构本地日期（避免 `'-'` 被当 UTC 的坑），取 `getDay()` 得基准星期几。
      - 每行记录把它的星期（key 第5段）转为 `WK[wk]`（周一=1…周五=5），算差值 `off = wkIdx - baseIdx`，
        A 的真实日期 = 基准日 + off 天，格式化为 `年/月/日`（不带零，如 `2026/9/3`）。→ 请 9/2(周三) 时周四行落到 9/3。
      - H 列保留 `today`（当天下载日期）；其余列（D 事由、I=1、J 午休 0.5）不变。
13. 【index】底部导航前两 tab「班级课表」与「个人课表」交换位置，且默认先显示「个人课表」：
    - 原顺序/tab：班级课表(class) / 个人课表(teacher) / 通讯录(contacts) / 统计(admin)，默认显示班级。
    - 改为：个人课表(teacher) / 班级课表(class) / 通讯录 / 统计，默认 active 在「个人课表」。
    - 实现（index.html）：对调两个 `.bnav-btn`（data-tab class↔teacher）HTML 顺序并把 active 移到 teacher；
      同时把面板默认 active 从 `#panel-class` 改到 `#panel-teacher`。切换仍靠 app.js 用 `data-tab→panel-id` 配对，面板 <section> 文档顺序可不动，逻辑零影响。
    - 文档序注：`#panel-class` 与 `#panel-teacher` 两个 <section> 仍 class 在前、teacher 在后，但没有 active 的不影响默认显示（显示仅由 .active 决定），故未移动 section 以降低大段误改风险。
14. 【标题】两页抬头统一为“大标题 + 下方留空副标位”的居中结构：
    - index.html：去掉原副标文案「2026 / 08 / 31 版」，保留 logo-sub 占位（留空 &nbsp;）。顶部现为大标题「课表速查助手」+ 空副标，居中。
    - daike.html：标题保持「代课管理助手」（其间曾临时试改为「代课管理」又改回，最终不变）；新增与首页一致的 logo-area/logo-sub 居中结构，副标留空占位。
    - 说明：两页 logo-area 用 flex-column + center，logo-text 下方一行空 logo-sub 保持两行结构（副标位置保留、内容为空）。editor 页头标题名称仍为「代课管理助手」。入口功能口径不变。
15. 【标题位置统一】daike 页顶部标题参照首页「课表速查助手」同款结构对其显示：
    - 把 daike.html 页头标题区块(header-inner/logo-area/logo-text/logo-sub)的 CSS 替换为与 index 首页完全一致：
      顶部 padding 15px16px10px、logo-area 垂直列中+margin-bottom12px+gap4、logo-text 字号统一 24px(line-height1.3、word-break)、logo-sub 14px 两行间距 margin-top2px。
    - 效果：两页顶部标题同为“页面居中 + 同一上下留白/行距/字号”的“大标题 + 空副标”排版，观感一致。
    - 注：mobile(i≤768/≤400) 已在 daike 补上与 index 同款缩放（≤768→20px、≤400→18px），两页手机标题字号亦一致。
    - 有周偏移正常的边界现象（见第八节12注：多选星期时每条 A 会按真实周几自然落在相邻不同日期）。
16. 【daike 移除“修改”面板 → 新增只读“通讯录”tab；通讯录更新至 233；拼音首字母映射补全】
    - daike 底部导航由（代课 / 修改 / 导入）改为（代课 / 通讯录 / 导入）：原可改课表/改电话的「修改」面板移除，**老师不再能通过 UI 改课表或改号码**。
    - 新增只读「通讯录」页（index 样式）：默认显示前 5 条 + “查看全部”，支持中文/拼音首字母搜索（复用 data-loader 的 `searchContacts`），号码可 `tel:` 直接拨打；无编辑能力。
    - 特别处理“数据就绪自愈”→ 若首载时 `contactsData` 尚未注入会先显示空并轮询补画（约 250ms×N + `__whenDataReady` 双保险），避免一进 tab 就停在“未找到教师”。
    - URL 锚点：`#通讯录` / `#contacts` 直达通讯录；旧 `#修改` / `#edit` 兼容重定向到通讯录。
    - 教师通讯录 `contactsData` 依 `tmp_new_contacts.txt` 重建为 **233 位**，写入独立文件 `js/contacts.json`（维护清单 tmp 不会自动生效，须把重建结果写回 contacts.json）。
    - PY_MAP（js/data-loader.js）补字母汇总：前批 `秋Q 风F 力L 晗H 欢H 妮N 钟Z 雯W 悦Y`＋本批 `连L 伊Y`；现覆盖 **251** 字、全清单缺首字母 = 0。
    - 技术债：原「修改」面板旧的可编辑 HTML/CSS/JS 仍留于 DOM（display:none、无入口、不可再进入）；如需彻底清理需连同其对应不再判空的事件绑定一并删除（改动较大，目前不影响任何功能）。
17. 【数据架构】课表与通讯录解耦：`data.json` 只留课表、名单独立到 `js/contacts.json`
    - `js/data.json` 移除 `contactsData`，现仅含 `scheduleData / periods / days`；新增 `js/contacts.json`（结构 `{ "contactsData":[...] }`，依 tmp 名单 233 位）。
    - `js/data-loader.js` 由原来“fetch 单文件（data.json）”改为 `Promise.all` 并行加载 data.json + contacts.json，两份都就绪才置 ready / 触发所有 `__whenDataReady` 回调并把原全局（scheduleData / contactsData / PY / search 等）照旧注入，页面（index / daike）无需改动。
    - daike「导入」改为产出**纯课表 data.json**：删除原“端内合并旧电话→生成 contactsArr”与 `contactsData` 字段，换新课表不再把名单重算/并回 data.json，名单也因而不会在换课表时被覆盖或丢失。
    - 发布注意：改课表/名单/loader 中任一处上线时，`js/data.json` 与 `js/contacts.json` 两个都要保留在站点（loader 并行加载缺一即整链失败）。
18. 【午休】课表中“午休”作为可选值班节接入（data.json / daike / 紧凑显示）
    - 语义：模板长期保留“午休”列；它是**可选节**，不占 1~6。有安排才显示，无安排忽略，开学初全空也不影响 1~6 正常使用。
    - 校内键：data.json 中存字符串键 `"午休"`（每个班每工作日一格 `{course,teacher,room}`）；无格子的班/天不建该键。
    - 导入：daike「导入」老格式解析已让“午休”列登记成一节（映射到入库的键 `'午休'`），有课程/教师数据才写。
    - daike 内统一用整数 **98** 表示午休：`getTeacherCourses` 会额外扫 `scheduleData[cls][day]['午休']` 并入课时；勾选格显示“午休”；累积/通知/导出继续可带出午休；导出 F 列 `periodName(98)→"午休"`。
    - 紧凑节次显示（mergePeriods 重写）：数字节即使不连续也揉成一个“第…节”串（如 `2,3,6 → 第2、3、6节`），午休一律放句尾用顿号连接（`第2、3、6节、午休`；仅午休 → `午休`）。影响全文通知与分段复制（共用同一函数）两处，措辞一致。
    - 打开方式：模板一直带午休列即可，暂时留空没关系；某班/某学期填了 teacher，就会自动出现在检索与通知。
    - 未做：首页 index.html 个人课表目前尚未单独排“午休”整行（属可选项，需要再做）。

## 九、待办
- [ ] 新学期换课表（仅替换 js/data.json，课表纯结构）；人员/号码如有变时再另更新 js/contacts.json
- [ ] 如需隐藏真实网址 → CNAME 自定义域名
- [ ] 如需限制普通老师只能代课不能改 → 加口令/权限管理
    > 现状补充：daike 的「修改」tab 已移除（详见第八节16），老师已不能通过 UI 改课表/改号码，页面侧已无编辑入口（只读通讯录在底部中部“通讯录”tab）。但这不是口令级鉴权——只是撤掉了 UI；如需真正的“只读老师只能代课、管理员可改”仍需加权限/口令（尚未实现）。

---

## 十、代课通知与累积结构（本地 js 现状；上传发布后即按此生效）

> 本章节为当前 `js/daike.js` 的真实实现口径，是最新基准。涉及通知排版、累积 key、分组合并时以本节为准（第六/八节相关旧文作废）。

### 10.1 累积记录 key（8 段，含起止）
- key 现为 8 段：`num | cls | teacher | bz | day | reason | start | end`
- 段义依次：班次｜班级｜教师｜是否班主任(0/1)｜星期(第1~5字)｜请假事由｜请假开始｜请假结束。
- `start / end` 存的是**生成该批时那次**「请假日期→开始/结束」输入框的 `YYYY-MM-DD`。
- 因此：同一教师、同一节次、跨不同批（不同起止）会形成不同 key；同批同起止同理由自动去重。
- 记录入口在 `doGenerateRecord()`：取当次 startDate/endDate 一并拼入第 7、8 段。

### 10.2 通知合并规则（请假教师）
- 同一「事由」+「开始」+「结束」三者都相同，才把该组教师姓名用顿号并到同一行：
  `张红梅、李四（病假 9/7 至 9/9）`
- 只要三者中任一项不同（尤其同理由但起止不同），就分列不同行，并各自带上它自己的时段。
- 请假行为一行一条，**顶格**，前面不带“请假：”等前缀，括号格式：`姓名（事由 M/D 至 M/D）`，时间段不补零、不含年份（半角「至」）。
- 例：
  - 张红梅 病假 `9/7–9/9` 与 李四 病假 `9/7–9/9`（同理由同起止）→ 合并成一行。
  - 张红梅 事假 `9/7–9/9` 与 张红芝 事假 `9/7–9/11`（同理由、结束不同）→ 拆成两行，各带各自时段。

### 10.3 通知正文排版（全文复制与「分段复制到微信」同一生成，格式一致）
- 标题：`【五年级 代课通知】`（已去掉标题里的起止日期，也去掉多余空格）。
- 请假区上文所述（按三键分组）。
- 请假大区与节次区之间空一行。
- 节次区**按星期分块**：星期名单独一行（如 `星期一`），其下该星期各班**各占一行并顶格**：
  - 班级格式为 `二2 / 五3`（**不带“班”**字）；
  - 同一天连续节次合并显示成 `第3、4节`；
  - 若该代课含班主任，行尾追加 ` 代班主任`。
  - 不同星期块之间各空一行。
- 正文最末：**整份仅一行**系统当天日期+星期，形如 `2026/9/4 星期四`（YYYY/M/D 星期X，不补零，星期由系统当天求得）。
  - 全文复制一路径在最末一行出现一次；「分段复制到微信」以该年级段调用同一生成，因此每段各自带一行同样日期。

### 10.4 附带说明 / 已知边界
- 因 key 末端多了 start/end，累积“条数”按“时段维度计”，同课程跨不同时段会另计一条（不影响通知生成逻辑，仅影响累积计数口径与刷新回显的一致性）。
- 手机微信为非等宽字体：请假/正文缩进等纯文本对齐只能以 5 个英文半角空格做近似，跨 App 无法 100% 逐列对齐，属平台限制。
- 大改这段期间曾为性能做过“移除全选/整日勾选循环内逐次重建累积栏”的低影响优化（不改界面、不改结果），如需沿用请在 doc 版本迭代记录补一条备注。
